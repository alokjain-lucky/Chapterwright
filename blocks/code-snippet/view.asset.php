<?php
/**
 * Script dependency manifest for view.js — no dependencies needed.
 *
 * @package Make_A_Book
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

return array(
	'dependencies' => array(),
	'version'      => MAKE_A_BOOK_VERSION,
);
